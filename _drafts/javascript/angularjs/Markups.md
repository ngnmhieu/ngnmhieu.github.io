## Directives

### Definition
Directive is one type of Angular markups. It apply extended behaviors to elements.

### Examples
  ng-app, ng-model, ng-controller,...
  Custom Directives

## {{ expression | filter }}

expression: js like code => read and write variables. Data come from "model".

filter: format the value of expression for display.
